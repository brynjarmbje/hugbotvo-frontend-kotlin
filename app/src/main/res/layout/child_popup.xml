<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Popup Title -->
    <TextView
        android:id="@+id/childPopupTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Leikmaður"
        android:textSize="20sp"
        android:textStyle="bold"
        android:gravity="center" />

    <!-- Filler Details -->
    <TextView
        android:id="@+id/childPopupContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Upplýsingar um leikmann"
        android:textSize="16sp"
        android:layout_marginTop="16dp" />

    <!-- Delete Child Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/deleteChildPopupButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Fjarlægja"
        app:cornerRadius="16dp"
        android:layout_marginTop="24dp" />

    <!-- Close Popup Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/closeChildPopupButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Loka"
        app:cornerRadius="16dp"
        android:layout_marginTop="16dp" />

</LinearLayout>
